namespace LeanPolyglot

def double (n : Nat) : Nat := n + n

lemma add_zero (n : Nat) : n + 0 = n := by
  simp

theorem prop_obligation : Prop := True

example (n : Nat) : n = n := by
  rfl

end LeanPolyglot
