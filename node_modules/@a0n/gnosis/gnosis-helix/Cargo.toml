[package]
name = "gnosis-helix"
version = "0.1.0"
edition = "2021"
description = "Canonical Rust port of the HELIX (Stoplight) corridor algebra from Gnosis.Civil.QuasicrystalStoplightTiming. SSOT for every FOIL runtime's scheduling math."
license = "MPL-2.0"

[lib]
name = "gnosis_helix"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
