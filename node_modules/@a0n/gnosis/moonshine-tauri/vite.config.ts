import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 1422,
    // Tauri waits for devUrl (localhost:1422); fail loudly if the port is taken
    // instead of silently picking another and leaving `tauri dev` hanging.
    strictPort: true,
    host: 'localhost',
  },
  build: {
    outDir: 'dist',
    target: 'esnext',
  },
});
