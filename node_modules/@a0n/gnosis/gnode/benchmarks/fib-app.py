def fibonacci(n):
    normalized = max(0, int(n))
    previous = 0
    current = 1
    for _ in range(normalized):
        previous, current = current, previous + current
    return previous


def app(input):
    return str(fibonacci(input.n))
