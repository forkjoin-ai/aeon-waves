rknots/*.knot
rknots/*.knot.spectral.tmp
rknots/*.knot.dense.tmp
.molmo-ref/
dinov2-path.pack
dinov2-path.knot
# watcher runtime state (markers + single-flight lock + launchd logs) —
# regenerable; must never be committed.
scripts/.mistral-watcher/
scripts/.kaiju-zoo-watcher/
# kaiju scratch/staging outputs (regenerable knot artifacts)
scripts/scratch-kaiju/
# tokenized wikitext blob for kaiju-ppl — regenerable via tokenize.py, never commit
*.ids.u32
