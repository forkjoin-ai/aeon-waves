import Lean.Helper
import Mathlib

namespace LeanPolyglot

def entryValue (n : Nat) : Nat := helperDouble n

theorem entryThm (n : Nat) : entryValue n = n + n := by
  simp [entryValue, helperDouble]

end LeanPolyglot
