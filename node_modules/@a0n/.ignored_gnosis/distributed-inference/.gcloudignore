# Submitting `gcloud builds submit .` from this directory alone does **not**
# include `../../bitwise` or `../gnosis-frf` — use `scripts/submit-paris-probe-cloud.sh`.
/target/
**/target/
**/*.log
**/*.tmp
**/.DS_Store
