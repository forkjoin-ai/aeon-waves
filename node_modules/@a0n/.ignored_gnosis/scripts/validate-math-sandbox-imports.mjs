#!/usr/bin/env node
/**
 * @deprecated Use `validate-init-only-import-closure.mjs` (config-driven Init-only roots).
 * Kept so existing docs and muscle memory keep working; forwards to the shared validator.
 */
import { spawnSync } from 'node:child_process';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const target = path.join(__dirname, 'validate-init-only-import-closure.mjs');
const r = spawnSync(process.execPath, [target], { stdio: 'inherit' });
process.exit(r.status ?? 1);
