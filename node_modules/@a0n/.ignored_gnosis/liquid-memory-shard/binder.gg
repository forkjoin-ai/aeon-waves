// binder.gg -- Forkjoin.ai binder registration for open-source/gnosis/liquid-memory-shard
// curated 2026-05-17 (originally auto-classified 2026-05-17)
// Spec: scripts/binder-registration.gg.md

(self: BinderProject {
  layer: 'kernel',
  cluster: 'gnosis-kernel',
  status: 'live',
  one_line: 'This track treats a **shard** as a byte-addressable pool (BRAM/DDR on FPGA, `Vec<u8>` in the lab) with a **fixed 10-byte AeonFlow** header on every message so framing overhead stays bounded next to fa',
  chapter_anchor: '60'
})

(self) -[:CLAIM { id: '70B-on-128MB-isolates' }]-> (claim_liquid: Evidence {
  text: 'Liquid memory sharding lets 70-billion-parameter models run across 128MB Cloudflare Worker isolates. Resident weight per worker is paged in/out as the FRF race demands; the model lives across the fabric.',
  evidence: 'open-source/gnosis/liquid-memory-shard/README.md'
})

(self) -[:CLAIM { id: 'shard-as-byte-pool' }]-> (claim_shard: Evidence {
  text: 'Shard treated as byte-addressable pool (BRAM/DDR on FPGA, Vec<u8> in lab) with fixed 10-byte AeonFlow header. Abstraction crosses substrates from software to hardware.',
  evidence: 'open-source/gnosis/liquid-memory-shard/README.md'
})
