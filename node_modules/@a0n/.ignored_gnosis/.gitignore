node_modules
dist/
target/
distributed-inference/target-flux-txt2img/
runtime/pkg/*
!runtime/pkg/gnosis_runtime.js

# Local secrets — never commit (R2 token etc., added 2026-06-04)
.env.local
**/.env*
!**/.env.example
!**/.env.sample
# Generated/decompressed model artifacts (exceed GitHub 100MB file limit)
distributed-inference/rknots/*.rknot
!runtime/pkg/gnosis_runtime.d.ts
!runtime/pkg/gnosis_runtime_bg.js
!runtime/pkg/gnosis_runtime_bg.wasm
!runtime/pkg/gnosis_runtime_bg.wasm.d.ts
!runtime/pkg/package.json
!runtime/pkg/README.md
runtime/target/
.lake/
.DS_Store
states/
*.bin
__pycache__/
.tmp-test-workspaces/gnode-test-history.json
forkjoin-app/.tsbuildinfo

# Mandatory monorepo ignores (synced by a0)
.wrangler/

# Local/generated model artifacts exceed GitHub's normal file limits.
distributed-inference/models/llama-70b.rknot

# Local benchmark and hardware QA captures are generated artifacts.
distributed-inference/.qa-artifacts/
# Over-the-air hexon-braid captures + IQ are regenerable (scripts/braid-over-air.sh).
.qa-artifacts/braid-over-air/

# Incremental typecheck cache (regenerable — tsconfig.typecheck.json)
*.tsbuildinfo
.tsbuildinfo-typecheck

# Test fixture byproducts from script-service/scalac runs.
harnesses/fixtures/.scala-build/
harnesses/fixtures/script-service-*

# Agent worktree scratch (never source)
.claude/worktrees/

.x-ranger/

# TwoTower P2 context datasets (the XL npz is ~293MB, exceeds GitHub's 100MB limit).
# Rebuild with `python3 twotower/train_p2_xl.py --extract` (deterministic corpus + mctx
# canary); the committed *.meta.json + rebuild note reproduce them. Never commit the npz.
twotower/*.npz
