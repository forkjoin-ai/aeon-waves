import { readFileSync } from 'fs';
import { resolve } from 'path';
import { initSync } from 'gnosis_runtime';

// Initialize the WASM module synchronously before any tests run
const wasmPath = resolve(
    import.meta.dir,
    '../../../../node_modules/.bun/gnosis_runtime@file+open-source+gnosis+runtime+pkg/node_modules/gnosis_runtime/gnosis_runtime_bg.wasm'
);
const wasmBytes = readFileSync(wasmPath);
initSync({ module: wasmBytes });
