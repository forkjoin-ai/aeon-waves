[package]
name = "gnode"
version = "0.1.0"
edition = "2021"
description = "Rust CLI that compiles TypeScript orchestrators into GG and runs them through Gnosis"

[[bin]]
name = "gnode"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
