import { readFileSync } from 'fs';
import { analyzeGnosisSource } from './src/analysis';
async function run() {
  const source = readFileSync('examples/benchmarks/moa-transformer-moa.gg', 'utf-8');
  const result = await analyzeGnosisSource(source);
  console.log(JSON.stringify(result.universe, null, 2));
}
run();
