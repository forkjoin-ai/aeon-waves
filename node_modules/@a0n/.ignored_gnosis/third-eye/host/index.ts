// @a0n/third-eye-host — the isomorphic (Node + Worker) host shim that binds the
// sovereign third-eye perception WASM (../pkg) into a service, zero-copy both
// directions. See third-eye-host.ts for the contract. The recon-server hot-binds
// it for the local node; the edge-worker wave (PerceptionStreamDO) consumes the
// same shim via fromWasm()/loadFromInit().
export {
  ThirdEyeHost,
  type ThirdEyeOpts,
  type ThirdEyeTelemetry,
  type ThirdEyeWasm,
} from './third-eye-host.ts';
